name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * 1' # every Monday 02:00 UTC (10:00 Taipei)

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v4

      - name: Cache Scryfall API responses
        uses: actions/cache@v4
        with:
          path: .scryfall-cache.json
          key: scryfall-${{ hashFiles('src/content/posts/**') }}
          restore-keys: |
            scryfall-

      - name: Install, build, and upload your site
        uses: withastro/action@v2
        with:
          node-version: '20'

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4
